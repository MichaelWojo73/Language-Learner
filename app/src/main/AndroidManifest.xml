<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.languagelearner">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainMenu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SpainishMainMenu" />
        <activity android:name=".SpanishAnimals" />
        <activity android:name=".SpanishFood" />
        <activity android:name=".PolishMainMenu" />
        <activity android:name=".polishGreetings" />
        <activity android:name=".SpanishSchool" />
        <activity android:name=".SpanishFamily"></activity>
    </application>

</manifest>